@model TrabajoFinalLabIV.ViewModel.ClubesViewModel

@{
    ViewData["Title"] = "Index";
}

<h1>CLUBES</h1>


<div class="d-inline-flex p-2 bd-highlight justify-content-between flex-wrap" style="width:100%">

    @foreach (var item in Model.Clubes)
    {
        @if (!string.IsNullOrEmpty(@item.ImagenEscudo))
        {
            <div class="card m-2" style="width: 16rem">
                <div class="d-flex align-items-center" style="height:280px">
                    <a asp-action="Details" asp-route-id="@item.Id">
                        <img src="/images/clubes/@item.ImagenEscudo" alt="@item.Nombre" class="card-img-top" style="max-height:300px" asp-append-version="true" />
                    </a>
                </div>
                <div class="card-body">
                    <h5 class="card-title">"@item.Nombre"</h5>
                </div>
                <div class="card-footer d-flex justify-content-center">
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary">Detalles</a>
                </div>
            </div>
        }
    }

</div>
